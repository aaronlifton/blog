import { test, expect } from "@playwright/test";

test("test", async ({ page }) => {
  // await page.goto("http://localhost:4321/");
  // await page.getByTitle("Ruby now produces machine").click();
  // await page.getByLabel("Open full-size image").click();
  // await page.getByLabel("Close", { exact: true }).press("Escape");
  // await page.getByRole("link", { name: "Home" }).click();
  // await page
  //   .getByRole("link", {
  //     name: 'Go to the post titled "Wezterm workspace switcher API"',
  //   })
  //   .click();
  // await page.getByLabel("Open full-size image").click();
  // await page.getByLabel("Close").click();
  // await page.getByLabel("Open full-size image").press("Escape");
  // await page.getByLabel("Open full-size image").press("Escape");
  // await page.getByLabel("Open full-size image").click();
  // await page.getByText("X Wezterm configuration file").click();
  // await page.getByText("X Wezterm configuration file").press("Escape");
  // await page.getByLabel("Open full-size image").press("Escape");
  // await page.getByRole("link", { name: "Blog", exact: true }).click();
  // await page.getByRole("link", { name: "next" }).click();
});
